cd to mobile

npx expo export -p web (this creates the dist folder)

npx serve dist


ngrok auth token: 3HSMJyrV7Njbu7Ijqqp2LbWoiao_7TSPJmmQSXdbJLphw4jX7


how to test deploy using ngrok

1. go to mobile directory then run: npx expo export -p web
2. npx serve -s dist -l 3000
3. open new terminal
4. npx serve -s dist -l 3000


to upload the new commit

upload everything to github

then cd to mobile and run
Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue
npx expo export -p web --clear

then recommit and upload new dist in render and netlify



env.local EXPO_PUBLIC_API_URL=http://localhost:5000/api
env.prod https://dcms-api-ljco.onrender.com/api


running local

make .env EXPO_PUBLIC_API_URL=http://localhost:5000/api
npm run dev
npx expo start
npx.cmd expo start --web -c




how to run the  website
cd api npm run dev

root folder

npx.cmd serve website -l 5173`